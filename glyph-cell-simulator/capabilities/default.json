{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the simulator window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
